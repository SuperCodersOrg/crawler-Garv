### Session 1
* Date: 9-07-2026 (9.30AM-12.30PM)

* Duration: 3 hours

* Goal: Building URL Normalizer

* Problem Encountered: had to implement stack operations for the url normalizers relative url handling 

* Outcome: built three new methods in dynamic array back,const back and pop back to handle the stack operations for the url normalizer

also built normalizer method in URL NORMALIZER and working on relativeresolve method


also faced a problem where 
```cpp
98% tests passed, 1 tests failed out of 65

Total Test time (real) =   1.43 sec

The following tests FAILED:
         33 - URLNormalizerTest.KeepNonDefaultPort (Failed)
Errors while running CTest
Output from these tests are in: C:/Users/garvc/Desktop/CQ/Project-02/build/Testing/Temporary/LastTest.log
Use "--rerun-failed --output-on-failure" to re-run the failed cases verbosely.

```

this test case was failing because i never set the result.hasport to false when the port was not present in the url, so the test case was failing because it was expecting result.hasport to be false but it was true. I fixed this issue by setting result.hasport to false when the port is not present in the url.

also faced some issues while cmake because i had a problem while cmake where i didnt include NormalizedURL.h was not included in the CMakeLists.txt file, so I added it to the CMakeLists.txt file and it worked fine after that.